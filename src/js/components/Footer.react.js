var React = require('react');

var Footer = React.createClass({
  render: function () {
    return (
      <div id="footer">
        <hr />
        A webapp for generating random words powered by React and Flux
      </div>
    );
  }
});

module.exports = Footer;
